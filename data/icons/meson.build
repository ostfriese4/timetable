scalable_dir = 'hicolor' / 'scalable'
install_data(
  scalable_dir / 'apps' / ('page.codeberg.ostfriese4.Untis.svg'),
  install_dir: get_option('datadir') / 'icons' / scalable_dir / 'apps'
)
install_data(
  scalable_dir / 'status' / ('starred-symbolic.svg'),
  install_dir: get_option('datadir') / 'icons' / scalable_dir / 'status'
)
install_data(
  scalable_dir / 'legacy' / ('system-users-symbolic.svg'),
  install_dir: get_option('datadir') / 'icons' / scalable_dir / 'status'
)